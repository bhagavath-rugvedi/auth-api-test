// Mock environment variables
process.env.JWT_SECRET = 'test_jwt_secret';

// Set up MongoDB memory server for testing if needed
// This is optional but recommended to avoid affecting your real database